MR Mobile V32

Fixes product detail click runtime error by removing references to missing detail DOM elements.
All existing product, cart, filter, admin, D1, gallery, review, and specs features are preserved.
